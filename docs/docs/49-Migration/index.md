# Migration Guides

You can find detailed migration documentation for each Stonecraft version that recommends changes on the side panel.

:::warning
Stonecraft `1.7.x` is end-of-life and receives no further updates. Please upgrade to `1.9+` (or `1.8.x` if you must stay on Stonecutter 0.7).
:::

## Changes tl;dr;

### Stonecraft 1.7.x → 1.8.x

- Stonecutter version is now 0.7.+
- The `vers()` function is deprecated in the `settings.gradle.kts`; use `version()` instead.
